/*
 * 2019 Spring Compiler Course Assignment 2 
 */


float c = -1.5;

bool loop(int n, int m, bool l, float k);
int fuck(string s1, string s2);
float fuck();
float foo();

int jjj(int h, int h)
{
    float kkk;

    return 8;
}


int main() {
    // Declaration
    int x;
    int i;
    int a = +5;
    int many = (x + i) / (a - loop(1, 9, 9, 7));
    int c = 2;
    string y = "She is a girl";

    print(y); // print

    // if condition
    if (a > 10) {
        float ggg;
        int c;
        x += a;
        print(x);
        if (true) {
            i++;
        }
        else if (true) {
            i++;
        }
    } else {
        float ggg;
        x = a % 10 + 10 * 7; /* Arithmetic */
        float ggg;
        print(x);
    }
    loop(x, i);
    print("Hello World");
    a = loop(x, i) * 4;
    a = 87 * loop(x, i);
    return 0; 
}

int fuck(string s1, string s2)
{
    float v = -9.2;

    return 1;
}

bool loop(int n, int m, bool l, float k) {
    while (n > m) {
        n--;
        while (true) {
            ;
        }
    }
    return true;
}

float foo()
{
    int x = 9;
}
